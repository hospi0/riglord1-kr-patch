# 리그로드 사가 (새턴 JP) 한글화 — 초기 조사

## 원본
- `C:\claude\roms\ss\Riglordsaga (Japan) (Made in Japan) (4M)\` — cue + 7트랙(Track 1 = MODE1/2352 데이터, 2‥7 = 오디오)
- Track 1: 177,406,656 B, md5 `4270c76f0492f0fc1cfec2254724f597`

## 참고할 기존 작업
- **리그로드 사가 2**(`riglord2-kr-patch`, 배포 v0.82) — 같은 시리즈·같은 개발사. 텍스트·글꼴·빌드 도구와 용어(드래군, 리그로드 대륙 등) 재사용 1순위. ★[[feedback_nul_padding_truncates_list]].
- 새턴 공통: 기존 새턴 프로젝트 docs 먼저(ISO9660·MODE1 ECC 재계산 등).

## 다음
- 파일 목록·텍스트/글꼴 위치 조사(리그로드 사가 2 구조와 비교).

## 1차 조사 (2026-09-25) — 리그로드 사가 2 와 거의 같은 구조
- 도구: 2편의 `iso9660.py`·`mato.py`·`fntc.py`·`cdrom_ecc.py` 복사. `tools/survey.py`(목록·매직), `tools/corpus.py`(분량).
- ISO 항목 949. 확장자: .BCH 219 · .ECH 163 · .MAP 102 · .SE 101 · .MAT 78 · **.ADV 76** · **.PRF 70** · **.P 64** · .BGM 23 · .DG2 22 … (.ADV/.PRF/.P 는 1편에만 있는 확장자로 보임). ⚠️항목 6개는 Track 1 끝 밖(CD-DA 를 가리킴) — 읽기에서 건너뜀.
- **mato 컨테이너 599개 — 2편 파서로 전수 통과**(오류 0).
- **FNTC 글꼴 = `/INO4INIT.DAT` +2764**(`/TITLE2.MAT`·`/TITLEMAT.GRF` 에도 FNTC 문자열 있음 — 미확인). ⚠️머리말이 2편과 다르다:
  `+0x04` 전체 크기(0xC290) · `+0x14` 글리프 시작(0xB90) · `+0x18` 글자 수 **1,464** · `+0x1C` **16×16 1bpp**(2편 16×12) · `+0x20` cp932 코드표(8140…) → 글리프 1,464×32 B. 크기 계산이 딱 맞음. 2편 `fntc.py` 는 머리 위치가 달라 그대로는 못 씀.
- **분량**(NUL 끝 cp932, 가나 30%↑ 또는 문장부호 포함, 중복 제거): 문자열 **4,976 · 전각 약 121,000자** — .MAP 3,950줄 98,801자(본편 대사, 2편과 같은 `%b`·`\n` 토큰) · .MSG 301줄 9,202 · .BIN 340줄 5,473(실행 파일 시스템 문구) · .MAT 34줄 3,130 · .ADV 68줄 2,875 · .BCH 207줄 1,010. 고유 전각 글자 1,306 < 글꼴 1,464칸.
- 비교: 2편 207,405자 / 9,043 문자열 → 1편은 약 60%.

## PoC 1 (2026-09-25) — 스크린샷 14장 화면 문구
- ★글꼴 FNTC 는 **세 벌**: /INO4INIT.DAT(1,464) · /TITLE2.MAT(1,461) · /TITLEMAT.GRF(1,378). 머리 +0x14 글리프 시작 · +0x18 개수 · +0x20 코드표 u16 **LE**. ★★글리프 = **16×12, 24 B**(행 2 B BE, 보이는 폭 12px) — 머리의 «10 10» 은 셀 크기일 뿐. 、。，．… 가 12행 간격인 것으로 확정(32 B 로 읽으면 뭉개진다).
- 한글 = «도너 코드»: 세 글꼴 공통 SJIS 한자(리드 0x88‥0x9F, 1,071개) 중 말뭉치에서 드문 것부터. 세 벌 모두 같은 배정(갈무리11, 2편 `krglyph.glyph` 재사용). 배정 스냅샷 `work/charmap.tsv`.
- 문구 위치: 메뉴·상태·옵션·인물 이름·소개·불러오기 = 실행 파일 **/1_SRPG.BIN**(UI 514000‥534000) + 같은 내용 **/2_SRPGED.BIN**(510000‥530000) + /0_OP.BIN(170000‥175000). ⛔실행 파일은 이 구간에만 쓴다 — 한 글자 문구(直·飛)가 코드 영역에서도 우연히 걸림.
- 기술 이름 = 1_SRPG/2_SRPGED 기술표(52 B 기록, 이름 필드 22 B) — **반각 가타카나**(ｽﾄﾗｲｸ) 또는 전각 SJIS(防御) 혼용 → 한글 도너 2 B 로 10자까지.
- 이름 입력 자판 = 1_SRPG @521268 부터 32 B 간격 10칸 줄(히라가나 11줄 + «カタカナ もどる 終了» + 가타카나 11줄 + «ひらがな …»). 가나·한자 칸 → 한글 음절(하·스·피·아·서 포함), 부호·영숫자 그대로.
- 대사: /QW00.MAP(화자 줄 `%H%m1%V100［%m0이름%m1］%V0%m0\n` 따로), /QW00.ADV, 기술 설명 /WAZAHELP.MSG.
- 빌드 `tools/build.py [--write] [--install]`: 제자리(예산 = 원문 바이트), 바뀐 섹터만 EDC/ECC 재계산(65/75,428). 번역 `work/ko/00_poc.tsv`(BIN·SKILL·/파일@오프셋). `work/rig1_kr_track1.bin` md5 `ab5daddb…` → F: 바깥 폴더 Track 1. ⏳실기.
- 추가(같은 날): ★오프닝 내레이션 = **/PROLO_00.DG2**(«PP» + u16 LE 가로/256·세로/256 + 팔레트 256×u16 BE + 8bpp, 1024×512, 6장면) → `tools/prolog.py`(연성체 18px·테두리 1·자간 2·그림자, 원본 회색 단계로 양자화, 크기 불변). ⛔문자열로는 없음(그림). «パワー» = 반각 «ﾊﾟﾜｰ:攻撃力+» 한 문자열의 머리(앞 바이트 NUL 또는 정렬 채움 0xFF) → 제자리 «파워». Track 1 md5 `ea9a5e99…` F: 설치. ⏳실기.
- 실기(2026-09-25): 오프닝 붓글씨 «아주 좋음». 이름 자판 첫 쪽 전환 줄이 «호후2 흐히 개»로 덮인 버그 — 자판 단계가 번역된 전환 줄(도너 = 한자 코드)을 한자로 착각 → 12·24번째 줄을 위치로 건너뛰게 고침. Track 1 md5 `ad31d642…` F: 설치.
- 컨트롤 설정 목록은 «문자열 바이트 길이» 기준 가운데 정렬 — 원본은 전부 12 B(=72px)라 왼쪽이 맞았다. 번역도 12 B(내용 앞·반각 공백 채움)로 맞춤. ★메뉴 항목은 원문과 «같은 바이트 수»로 채워야 정렬이 유지된다(NUL 로 짧게 끝내면 가운데 정렬이 달라짐). Track 1 md5 `416d3727…` F: 설치.
- ★자판 전환 줄(«　 カタカナ   　もどる   終了»)은 게임이 «글자 자리»로 버튼 범위를 판단하는 것으로 보임 — 글자 수가 다른 «한글2 뒤로 끝» 으로 바꿨더니 지우기 버튼이 커서만 뒤로 보내다 사라짐(실기). 원문과 같은 자리 구조(글자마다 2 B·공백 자리 유지)로: «한글２쪽 · 지우기 · 완료». Track 1 md5 `333cfffe…`. ⏳실기.

## 그림 글자 전수 확인 (2026-09-25)
- DG2 22개: PROLO_00(오프닝 내레이션 — ✅한글화) · PROLO_02·SKY·SKYBG(배경, 글자 없음) · RADAR·SKYMAP(6,854 B 작은 지도) · **STAFF_01‥16(엔딩 제작진 명단, 320×224 — 직함 가타카나 + 이름 붓글씨 한자, 로고·저작권은 영어)**. ⚠️STAFF 머리는 «PP 01 40 00 E0» = 가로 0x140(320)·세로 0xE0(224) 픽셀(PROLO 는 256 단위) — 형식이 둘.
- 동영상 RIG_OP/RIG_ED/RIG_EDS.CPK(Sega FILM·Cinepak, ffmpeg 로 프레임 확인): 일본어 글자 없음(«MICRO CABIN CORP.» 영어뿐).
- 불러오기 화면: 텍스트는 한글 확인(실기), «SELECT! LOAD MENU» 그림은 영어.
- 남은 미확인: 타이틀 화면(TITLE2.MAT·TITLEMAT.GRF 그림 블록) — 화면 확인 필요.
- 엔딩 제작진 명단 직함 한글화(사용자 결정: 직함만, 이름·로고는 그대로) — `tools/staff.py`, STAFF 12장, 직함 상자는 격자 확대 그림에서 손으로 지정(자동 검출은 뒤쪽 장에서 이름과 섞임), 갈무리9·자간 3·**금색 한 가지**(사용자 지정). 비교 그림 `my files/그래픽/02_엔딩직함(왼원본_오른한글).png`. ⏳실기.
- 엔딩 제작진 명단 직함 한글화(사용자 결정: 직함만, 이름·로고는 그대로) — `tools/staff.py`, STAFF 12장. 직함 상자는 격자 확대 그림에서 손으로 지정(자동 검출은 뒤쪽 장에서 이름과 섞임), 갈무리9·자간 3. ★색은 **모든 장 같은 금색 하나**(=«게임 디자인» 색 RGB555 20,18,12 → 160,144,96, 사용자 지정) — 팔레트에 그 색이 없으면 안 쓰는 칸을 그 색으로 바꿈(6·7·8번 장). 비교 그림 `my files/그래픽/02_엔딩직함(왼원본_오른한글).png`. ⏳실기.
