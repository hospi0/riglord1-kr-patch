# 리그로드 사가 1 한글화 — 이어하기(클라우드·새 세션용)

비공개 저장소. ROM·세이브스테이트·빌드 결과물은 없다(로컬에만). **클라우드에서는 번역만** 하고, 빌드·실기는 로컬에서 한다.

## 1. 현재 상태 (2026-09-25)
- ✅ 구조 조사 끝 — 리그로드 사가 2 와 거의 같다(mato 컨테이너·.MAP 대사·FNTC 글꼴). 상세 `docs/01_초기조사.md`.
- ✅ PoC 1: 스크린샷 14장 화면 문구 + 오프닝 내레이션 그림(`/PROLO_00.DG2`) + 이름 입력 자판 → 로컬 빌드·F: 설치 ⏳실기.
- 다음 = **본번역**(`work/text/*.tsv` 5,162줄).

## 2. 번역할 텍스트 (`work/text/*.tsv` — 전량 추출, 같은 문장은 한 줄)
| 파일 | 내용 | 줄 | 대략 글자 수 |
|---|---|---|---|
| map.tsv | 본편 대사(.MAP) | 4,008 | 99,636 |
| msg.tsv | 설명·도움말(.MSG .MAT .ADV) | 403 | 15,213 |
| ui.tsv | 실행 파일 UI(메뉴·상태·시스템 문구·인물 소개) | 533 | 6,630 |
| skill.tsv | 기술 이름(반각 가타카나 섞임) | 218 | 1,222 |

열: 번호 · 종류 · 자리수(같은 문장이 나오는 곳 수) · **예산(바이트)** · 예시위치 · JP · **KO(비어 있음)**.
- 작업 방식: `work/text/X.tsv` 를 `work/ko/X.tsv` 로 복사해 KO 열을 채운다. 배치마다 `python tools/kocheck.py`.
- `work/ko/00_poc.tsv` 는 PoC(형식이 다름: 대상·JP·KO). 본번역 파일과 같은 문장이 겹치면 본번역을 우선하도록 빌더를 맞출 예정 — PoC 줄도 다시 번역해도 된다.

## 3. 조판 규칙 (검사기가 막는다)
- **바이트 예산**: 원문 자리에 제자리로 덮어쓴다. 한글 1자 = 2 B, ASCII(반각 공백·영숫자·`%b` 등) = 1 B, 전각 부호 = 2 B. ★띄어쓰기는 **반각 공백**(1 B, 화면 6px — 2편 실측).
- **줄 폭**: `\n` 으로 나뉜 각 줄은 원문에서 가장 넓은 줄보다 넓으면 안 된다(전각·한글 12px, 반각 6px). 줄은 `\n` 으로 직접 끊는다.
- **제어 토큰**은 원문과 같게: `%b`(대기) `%h` `%H` `%m0` `%m1` `%V0` `%V100` `%s0` … 화자 줄 `%H%m1%V100［%m0이름%m1］%V0%m0\n` 은 이름만 바꾼다.
- 문장부호(, . ! ? : ;) 뒤 공백은 **빌더가 지운다**(전 프로젝트 규칙). 느낌표·물음표는 반각(`!` `?`)을 쓰면 1 B 로 줄어든다.
- 번역에 가나·한자를 남기지 말 것(글꼴 한자 칸을 한글이 쓴다).
- 일본어 고유명사는 거센소리(か=카, た=타, つ=츠).
- 용어는 **리그로드 사가 2 한글판**과 맞춘다: 리그로드 · 겐유사이 · 퀸즈랜드 · 드래군 · 아서 … PoC 에서 정한 것은 `work/ko/00_poc.tsv` 참고(인물: 아서·무사시·엘리제·헥터 경·마토프·시시마루·아케비·쿠로야샤·위스코·에반·샤르·애쉬·디아나·다이간).
- 번역 줄은 셸 이스케이프 말고 편집기로 직접 쓸 것(음절이 깨진다).

## 4. 도구
- `tools/export.py` — 추출(로컬, ROM 필요) → work/text.
- `tools/kocheck.py` — 번역 검사(ROM 불필요).
- `tools/build.py [--write] [--install]` — 빌드(로컬). 도너 코드 배정 → 글꼴 세 벌 → 제자리 덮어쓰기 → Track 1 ECC.
- `tools/prolog.py` — 오프닝 내레이션 그림(`/PROLO_00.DG2`) 한글판(장면 6개 번역이 파일 안에 있다).
- `tools/strindex.py` — 문자열 위치 색인·찾기.
